# Utils

UtilsModule. Useful utilities that can be used across the application

This directory contains:

- UtilsModule
- `axios` directory with AxiosWrapperModule which is a wrapper for HttpModule
- `client` directory with ClientService that helps with handling microservice requests
