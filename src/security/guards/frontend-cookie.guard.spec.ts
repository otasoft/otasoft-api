import { FrontendCookieGuard } from './frontend-cookie.guard';

describe('FrontendCookieGuard', () => {
  it('should be defined', () => {
    expect(new FrontendCookieGuard()).toBeDefined();
  });
});
